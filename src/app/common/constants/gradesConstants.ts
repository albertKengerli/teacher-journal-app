export enum GradesConstants {
  MaxGrade = 10,
  MinGrade = 0,
  PositivenessBorder = 5,
}

export enum GradeOperations {
  Delete = "Delete grade",
  Post = "Post grade",
  Update = "Update grade",
  Remove = "Remove operation"
}
